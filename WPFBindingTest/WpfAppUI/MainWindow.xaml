<Window x:Class="WpfAppUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel x:Name="stackpanel1">
            <TextBox Text="{Binding Path=ID}"></TextBox>
            <TextBox Text="{Binding Path=Name}"></TextBox>
        </StackPanel>
        <StackPanel x:Name="stackpanel2" VerticalAlignment="Center">
            <TextBox Text="{Binding Path=ID}"></TextBox>
            <TextBox Text="{Binding Path=Name}"></TextBox>
        </StackPanel>
    </Grid>
    <!--<Grid>
        --><!--<StackPanel>
            <TextBox x:Name="textBox1" Text="Dung dep trai qua thi phai"></TextBox>
            <Label x:Name="label1">
                <Label.Content>
                    <Binding ElementName="textBox1" Path="Text" Mode="OneWay"/>
                </Label.Content>
            </Label>
        </StackPanel>--><!--
    </Grid>-->
</Window>
